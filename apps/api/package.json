{
  "name": "@evidentia/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@evidentia/core": "workspace:*",
    "@evidentia/search": "workspace:*",
    "@evidentia/sources": "workspace:*",
    "@evidentia/verify": "workspace:*"
  }
}
